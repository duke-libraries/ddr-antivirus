stages:
  - build
  - test

build_23:
  stage: build
  before_script:
    - cd .docker/ruby23
  script:
    - docker-compose build

test_23:
  stage: test
  before_script:
    - cd .docker/ruby23
  script:
    - docker-compose run --rm app bundle exec rake

build_24:
  stage: build
  before_script:
    - cd .docker/ruby24
  script:
    - docker-compose build

test_24:
  stage: test
  before_script:
    - cd .docker/ruby24
  script:
    - docker-compose run --rm app bundle exec rake

build_25:
  stage: build
  before_script:
    - cd .docker/ruby25
  script:
    - docker-compose build

test_25:
  stage: test
  before_script:
    - cd .docker/ruby25
  script:
    - docker-compose run --rm app bundle exec rake
